package es.voghdev.playbattlegrounds

import org.junit.Test

import org.junit.Assert.assertEquals

class ExampleUnitTest {
    @Test
    @Throws(Exception::class)
    fun `addition is correct`() = assertEquals(4L, (2L + 2L))
}